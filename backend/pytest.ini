[pytest]
markers =
    calibration: makes real OpenAI API calls and costs money; not run by default
# Calibration is opt-in: CI and the deploy must never spend money running tests.
addopts = -m "not calibration"
